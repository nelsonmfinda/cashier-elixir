services:
  cashier:
    build:
      context: .
      target: runtime
    container_name: cashier_app
    environment:
      - MIX_ENV=prod
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "bin/cashier", "pid"]
      interval: 30s
      timeout: 5s
      retries: 3

  test:
    build:
      context: .
      target: test
    container_name: cashier_test
    command: mix test
    profiles:
      - test
